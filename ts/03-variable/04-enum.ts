// 100 created, 101 process, 102 finish.
// enum STATUS {n, n + 1, n + 2};       // default
// enum STATUS {x, y, z};               // Assign

// enum STATUS {CREATED = 100, PROCESS, FINISH};
// enum STATUS {CREATED = 100, PROCESS = 200, FINISH = 300};

// let todoStatus: STATUS = STATUS.PROCESS;

// console.log(todoStatus);    


